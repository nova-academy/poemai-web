"""Importing modules"""
import os
from django.shortcuts import render
from django.http import request
import requests


# Create your views here.
def index(request):
    """Renders page with form for user input"""


def generate(request):
    """Renders page with the generated poem"""
